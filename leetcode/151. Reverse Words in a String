class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """
        lst = s.split(' ')
        lst = [word for word in lst if word != '']
        return ' '.join(lst[::-1])
